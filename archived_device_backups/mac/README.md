# Mac (Device 3: Ultimate Fallback / Active-Active Sync)
The Mac acted as the final failover. It runs the main web UI in standby mode and connects to ADB over Wi-Fi.

## Key Files:
- `server.py`, `demo_server.py`, `scheduler.py`
- `run_daemon.sh`
- `database.py`, `replicate.py`
